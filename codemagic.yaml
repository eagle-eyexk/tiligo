workflows:
  ios-release:
    name: iOS Release (Capacitor Clean Build)
    instance_type: mac_mini_m1
    max_build_duration: 60

    environment:
      node: 22
      xcode: latest
      cocoapods: default
      groups:
        - app_store_credentials

    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main

    scripts:
      - name: Install npm dependencies
        script: |
          set -e
          npm ci --prefer-offline 2>&1 | tail -20

      - name: Ensure Capacitor packages
        script: |
          set -e
          npm install \
            @capacitor/core@^7.0.0 \
            @capacitor/cli@^7.0.0 \
            @capacitor/app@^7.0.0 \
            @capacitor/ios@^7.0.0 \
            @capacitor/haptics@^7.0.0 \
            @capacitor/keyboard@^7.0.0 \
            @capacitor/status-bar@^7.0.0 \
            --save 2>&1
          npx cap --version

      - name: Build web app
        script: |
          set -e
          npm run build 2>&1 | tail -30

      - name: Add iOS platform
        script: |
          set -e
          if [ ! -d "ios" ]; then
            npx cap add ios 2>&1
          fi

      - name: Sync Capacitor to iOS
        script: |
          set -e
          npx cap sync ios 2>&1 | tee "$CM_BUILD_DIR/cap-sync.log"

      - name: Set iOS deployment target to 14.0
        script: |
          set -e
          cd ios/App
          sed -i '' "s/platform :ios, '[0-9]*\.[0-9]*'/platform :ios, '14.0'/" Podfile
          if ! grep -q "IPHONEOS_DEPLOYMENT_TARGET" Podfile; then
            cat >> Podfile << 'PODEOF'

post_install do |installer|
  installer.pods_project.targets.each do |target|
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '14.0'
    end
  end
end
PODEOF
          fi
          echo "=== Podfile updated ==="
          cat Podfile

      - name: Clean previous pod artifacts
        script: |
          set -e
          cd ios/App
          rm -rf Pods Podfile.lock
          pod deintegrate 2>/dev/null || true

      - name: Install CocoaPods
        script: |
          set -e
          cd ios/App
          gem install cocoapods --no-document 2>&1 | tail -3
          pod install --repo-update 2>&1 | tee "$CM_BUILD_DIR/pod-install.log"
          if grep -qiE "error|failed|\[!\]" "$CM_BUILD_DIR/pod-install.log"; then
            echo "=== POD INSTALL ERRORS ==="
            grep -iE "error|failed|\[!\]" "$CM_BUILD_DIR/pod-install.log"
            exit 1
          fi

      - name: Verify workspace
        script: |
          set -e
          if [ ! -d "ios/App/App.xcworkspace" ]; then
            echo "ERROR: App.xcworkspace not found"
            ls -la ios/App/
            exit 1
          fi
          echo "Workspace verified"

      - name: Set build number
        script: |
          BUILD_NUMBER=$(( $(date +%s) - 1700000000 ))
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $BUILD_NUMBER" \
            ios/App/App/Info.plist 2>/dev/null || true

      - name: Build iOS app
        script: |
          set -e
          xcodebuild \
            -workspace ios/App/App.xcworkspace \
            -scheme App \
            -configuration Release \
            -archivePath "$CM_BUILD_DIR/build/App.xcarchive" \
            archive 2>&1 | tee "$CM_BUILD_DIR/xcodebuild.log"
          if [ ! -d "$CM_BUILD_DIR/build/App.xcarchive" ]; then
            echo "ERROR: Archive not created"
            tail -80 "$CM_BUILD_DIR/xcodebuild.log"
            exit 1
          fi

      - name: Export IPA
        script: |
          set -e
          xcodebuild \
            -exportArchive \
            -archivePath "$CM_BUILD_DIR/build/App.xcarchive" \
            -exportPath "$CM_BUILD_DIR/build" \
            -exportOptionsPlist ios/App/ExportOptions.plist 2>&1 || \
          xcodebuild \
            -exportArchive \
            -archivePath "$CM_BUILD_DIR/build/App.xcarchive" \
            -exportPath "$CM_BUILD_DIR/build" \
            -exportOptionsPlist "$CM_BUILD_DIR/ExportOptions.plist" 2>&1

    artifacts:
      - build/*.xcarchive
      - build/*.ipa
      - cap-sync.log
      - pod-install.log

    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_API_KEY
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        key_id: $APP_STORE_CONNECT_KEY_ID
        submit_to_testflight: true
        submit_to_app_store: false